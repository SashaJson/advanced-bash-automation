In CLI execute 

```
cd ~
```

```
stat .bash_profile
```

```
touch .bash_profile
```

```
vim .bash_profile
```

For example write in bash\_profile:
```
alias git_sync="git pull -r && git push"
alias ll="ls -laG"
```

