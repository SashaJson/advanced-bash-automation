In CLI execute 

```bash
cd ~
```

```bash
stat .bash_profile
```

```bash
touch .bash_profile
```

```bash
vim .bash_profile
```

For example write in bash\_profile:
```bash
alias git_sync="git pull -r && git push"
alias ll="ls -laG"
```

