In CLI execute commands:
```bash
date
```
For rerun last command
```bash
!!
```
If command need 'sudo':
```bash
ifconfig en0 down
```
```bash
sudo !!
```

For use last element in last command:
```bash
touch script.sh
```
```bash
chmod +x !$
```

